version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - FIGMA_API_TOKEN=${FIGMA_API_TOKEN}
    volumes:
      - /app/node_modules
    restart: unless-stopped
